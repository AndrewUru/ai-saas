{
  "$schema": "../../schemas/weekly-ui-improvement.schema.json",
  "id": "academy-empty-state-context",
  "status": "proposed",
  "priority": "P1",
  "area": "Academy",
  "category": "empty-state",
  "title": "Make the Academy empty state match its cause",
  "problem": "The Academy always tells users to clear filters and shows a Clear filters button, even when the library itself has no published resources and there are no filters to clear.",
  "solution": "Show filter-recovery guidance only when filters are active, and show a distinct no-content message with a contact action when the library is genuinely empty.",
  "acceptance": [
    "Active filters produce a no-results message with a working Clear filters action.",
    "An empty unfiltered library explains that resources are not available yet.",
    "The unfiltered empty state offers a relevant contact action instead of a no-op button."
  ],
  "createdAt": "2026-08-27T00:00:00.000Z",
  "operations": [
    {
      "type": "replace_text",
      "path": "app/academy/AcademyLibrary.tsx",
      "description": "Split the Academy empty state into filtered-no-results and genuinely-empty variants with relevant actions.",
      "expectedOccurrences": 1,
      "before": "      {filtered.length === 0 ? (\n        <div className=\"flex min-h-72 flex-col items-center justify-center rounded-2xl border border-dashed border-border bg-surface/20 p-8 text-center\">\n          <span className=\"flex h-12 w-12 items-center justify-center rounded-full border border-border bg-surface text-accent\">\n            <Search className=\"h-5 w-5\" aria-hidden=\"true\" />\n          </span>\n          <h3 className=\"mt-4 font-semibold text-foreground\">\n            No resources found\n          </h3>\n          <p className=\"mt-2 max-w-sm text-sm leading-6 text-[var(--foreground-muted)]\">\n            Try a broader search or clear the active filters to see the full\n            Academy library.\n          </p>\n          <button\n            type=\"button\"\n            onClick={clearFilters}\n            className=\"ui-button ui-button--ghost mt-6\"\n          >\n            Clear filters\n          </button>\n        </div>\n      ) : (",
      "after": "      {filtered.length === 0 ? (\n        <div className=\"flex min-h-72 flex-col items-center justify-center rounded-2xl border border-dashed border-border bg-surface/20 p-8 text-center\">\n          <span className=\"flex h-12 w-12 items-center justify-center rounded-full border border-border bg-surface text-accent\">\n            <Search className=\"h-5 w-5\" aria-hidden=\"true\" />\n          </span>\n          <h3 className=\"mt-4 font-semibold text-foreground\">\n            {hasActiveFilters ? \"No resources match your filters\" : \"No resources available yet\"}\n          </h3>\n          <p className=\"mt-2 max-w-sm text-sm leading-6 text-[var(--foreground-muted)]\">\n            {hasActiveFilters\n              ? \"Try a broader search or clear the active filters to see the full Academy library.\"\n              : \"New practical guides are being prepared. Tell us which topic would help you most.\"}\n          </p>\n          {hasActiveFilters ? (\n            <button\n              type=\"button\"\n              onClick={clearFilters}\n              className=\"ui-button ui-button--ghost mt-6\"\n            >\n              Clear filters\n            </button>\n          ) : (\n            <Link href=\"/contact\" className=\"ui-button ui-button--ghost mt-6\">\n              Suggest a topic\n            </Link>\n          )}\n        </div>\n      ) : ("
    }
  ],
  "proposedAt": "2026-08-27T21:05:51.539Z",
  "files": [
    "app/academy/AcademyLibrary.tsx"
  ]
}
